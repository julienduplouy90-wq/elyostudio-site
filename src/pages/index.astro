---
import Base from '../layouts/Base.astro';
import Icone from '../components/Icone.astro';
import Maquette from '../components/Maquette.astro';
import BandeCta from '../components/BandeCta.astro';
import {
  site,
  prix,
  problemes,
  etapes,
  activation,
  mensuel,
  formules,
  pourQui,
  faqs,
  lienDemarrer,
} from '../data/site.js';

const titre = 'Être trouvé, compris et choisi | ElyoStudio';
const description = `Accompagnement pour thérapeutes, coachs et praticiens : site professionnel, prise de rendez-vous, relances et visibilité locale. ${prix.activation} puis ${prix.mensuel}/mois.`;

const resultats = [
  'Un agenda plus régulier, moins dépendant du seul bouche-à-oreille',
  'Être trouvé par les personnes qui cherchent un praticien dans votre ville',
  'Une offre et des tarifs que vous présentez sans hésiter',
  'Des décisions prises en quelques heures, pas en quelques semaines',
];

const donnees = {
  '@context': 'https://schema.org',
  '@graph': [
    {
      '@type': 'FAQPage',
      '@id': `${site.url}/#faq`,
      mainEntity: faqs.map((f) => ({
        '@type': 'Question',
        name: f.q,
        acceptedAnswer: { '@type': 'Answer', text: f.a },
      })),
    },
    {
      '@type': 'Service',
      '@id': `${site.url}/#accompagnement`,
      name: 'Elyo Croissance — visibilité locale et développement d’activité',
      serviceType:
        'Site internet, prise de rendez-vous en ligne, relances, référencement local et accompagnement mensuel',
      provider: { '@id': `${site.url}/#studio` },
      areaServed: { '@type': 'Country', name: 'France' },
      audience: {
        '@type': 'Audience',
        audienceType: 'Thérapeutes, psychopraticiens, coachs, énergéticiens, professeurs de yoga et professionnels de l’accompagnement',
      },
      offers: {
        '@type': 'Offer',
        price: '690',
        priceCurrency: 'EUR',
        description: `${prix.activation} d’activation, puis ${prix.mensuel} par mois, sans engagement de durée.`,
        url: `${site.url}/offre`,
      },
    },
  ],
};
---

<Base titre={titre} description={description}>
  <script type="application/ld+json" is:inline set:html={JSON.stringify(donnees)} />

  <!-- ===== HÉROS ===== -->
  <section class="heros">
    <span class="heros-arc" aria-hidden="true"></span>

    <div class="rail heros-rail">
      <div class="heros-texte">
        <span class="heros-surtitre reveal">
          Thérapeutes · psychopraticiens · coachs · énergéticiens · professeurs de yoga
        </span>

        <h1 class="reveal" style="--delai: 90ms">
          Votre travail est sérieux.<br />Je fais en sorte que cela se voie.
        </h1>

        <p class="heros-chapeau reveal" style="--delai: 180ms">
          Vous êtes bon dans votre métier — encore faut-il qu’on vous trouve, qu’on comprenne ce que
          vous proposez, et qu’on puisse prendre rendez-vous facilement. C’est exactement ce que
          j’installe avec vous, puis que je fais évoluer mois après mois.
        </p>

        <div class="groupe-btn reveal" style="--delai: 260ms">
          <a href="/offre" class="btn btn-principal btn-large">
            Voir si Elyo Croissance est adapté à mon activité
            <Icone nom="fleche" classe="fleche" />
          </a>
          <a href="/methode" class="btn btn-clair btn-large">Voir comment ça fonctionne</a>
        </div>

      </div>

      <div class="heros-visuel reveal" style="--delai: 200ms">
        <Maquette />
      </div>
    </div>
  </section>

  <!-- ===== BANDEAU ===== -->
  <section class="chiffres">
    <div class="rail chiffres-grille">
      <div class="chiffre reveal">
        <strong>Sans engagement</strong>
        <span>l’abonnement mensuel s’arrête quand vous le décidez.</span>
      </div>
      <div class="chiffre reveal" style="--delai: 80ms">
        <strong>Un site inclus</strong>
        <span>compris dans l’activation, pas facturé à part.</span>
      </div>
      <div class="chiffre reveal" style="--delai: 160ms">
        <strong>1 interlocuteur</strong>
        <span>Julien, du premier échange au dernier suivi.</span>
      </div>
    </div>
  </section>

  <!-- ===== PROBLÈME ===== -->
  <section class="section">
    <div class="rail">
      <div class="entete-section reveal">
        <span class="surtitre">Ce que vivent la plupart des praticiens</span>
        <h2>Bon dans votre pratique, invisible en dehors du cabinet</h2>
        <p class="chapeau">
          Les praticiens que je rencontre ne manquent ni de compétence ni de sérieux. Il leur
          manque un système : être trouvé, être compris, et transformer ça en rendez-vous —
          sans y passer leurs soirées. C’est ce que je mets en place avec vous.
        </p>
      </div>

      <div class="grille grille-3 espace-haut">
        {
          problemes.map((p, i) => (
            <article class="carte carte-relief reveal" style={`--delai: ${i * 90}ms`}>
              <span class="pastille pastille-terre">
                <Icone nom={p.icone} />
              </span>
              <h3>{p.titre}</h3>
              <p>{p.texte}</p>
            </article>
          ))
        }
      </div>
    </div>
  </section>

  <!-- ===== RÉSULTAT RECHERCHÉ ===== -->
  <section class="section section-teintee">
    <div class="rail resultat-rail">
      <div class="entete-section reveal">
        <span class="surtitre">Ce que je construis avec vous</span>
        <h2>Un cabinet qu’on trouve, qu’on comprend, et qu’on choisit</h2>
        <p class="chapeau">
          Pas un site de plus : un système complet, que je mets en place dans le bon ordre, que
          vous gardez et que vous savez faire vivre.
        </p>
      </div>
      <ul class="liste-cochee resultat-liste reveal" style="--delai: 120ms">
        {resultats.map((r) => <li>{r}</li>)}
      </ul>
    </div>
  </section>

  <!-- ===== CE QUE NOUS INSTALLONS ===== -->
  <section class="section section-sombre" id="activation">
    <span class="halo halo-anime direct-halo"></span>
    <div class="rail">
      <div class="entete-section reveal">
        <span class="surtitre surtitre-clair">{activation.surtitre}</span>
        <h2 class="titre-clair">{activation.titre}</h2>
        <p class="direct-chapeau">{activation.texte}</p>
      </div>

      <div class="socle-grille espace-haut">
        {
          activation.points.map((p, i) => (
            <article class="socle-carte reveal" style={`--delai: ${i * 60}ms`}>
              <span class="direct-icone"><Icone nom={p.icone} /></span>
              <div>
                <h3>{p.titre}</h3>
                <p>{p.texte}</p>
              </div>
            </article>
          ))
        }
      </div>
    </div>
  </section>

  <!-- ===== CE QUI ÉVOLUE CHAQUE MOIS ===== -->
  <section class="section">
    <div class="rail">
      <div class="entete-section reveal">
        <span class="surtitre">{mensuel.surtitre}</span>
        <h2>{mensuel.titre}</h2>
        <p class="chapeau">{mensuel.texte}</p>
      </div>

      <div class="grille grille-3 espace-haut">
        {
          mensuel.points.map((p, i) => (
            <article class="carte carte-relief reveal" style={`--delai: ${i * 70}ms`}>
              <span class="pastille"><Icone nom={p.icone} /></span>
              <h3>{p.titre}</h3>
              <p>{p.texte}</p>
            </article>
          ))
        }
      </div>

      <div class="reveal centre-mobile espace-haut-petit">
        <a href="/offre" class="lien-fleche">
          Voir Elyo Croissance en détail
          <Icone nom="fleche" classe="fleche" />
        </a>
      </div>
    </div>
  </section>

  <!-- ===== DIFFÉRENCIATION ===== -->
  <section class="section">
    <div class="rail">
      <div class="entete-section reveal">
        <span class="surtitre">Ce qui change avec Elyo</span>
        <h2>Ni une agence classique, ni une formation, ni un consultant</h2>
        <p class="chapeau">
          Ces trois approches échouent souvent au même endroit : vous laisser seul au moment de
          faire. Elyo Direct combine les trois choses qui ne fonctionnent pas séparées — la
          stratégie, la mise en œuvre réelle et l’accompagnement dans la durée.
        </p>
      </div>
      <div class="grille grille-3 espace-haut">
        <article class="carte carte-relief reveal">
          <span class="pastille"><Icone nom="gabarit" /></span>
          <h3>Pas une agence qui livre puis disparaît</h3>
          <p>
            Le site n’est pas la fin de la mission, c’est le début. Une fois en ligne, on le fait
            travailler : visibilité, contenu, ajustements, mois après mois.
          </p>
        </article>
        <article class="carte carte-relief reveal" style="--delai: 90ms">
          <span class="pastille"><Icone nom="video" /></span>
          <h3>Pas une formation en vidéos</h3>
          <p>
            Je ne vous laisse pas apprendre un deuxième métier. On réfléchit ensemble, puis je
            construis réellement : pages, réservation, fiche Google, automatisations.
          </p>
        </article>
        <article class="carte carte-relief reveal" style="--delai: 180ms">
          <span class="pastille"><Icone nom="document" /></span>
          <h3>Pas un consultant qui recommande sans faire</h3>
          <p>
            Les recommandations sont mises en place, pour de vrai. Et chaque mois, on ajuste selon ce
            que disent les résultats — pas selon le plan initial.
          </p>
        </article>
      </div>
    </div>
  </section>

  <!-- ===== DÉROULÉ ===== -->
  <section class="section">
    <div class="rail">
      <div class="entete-section reveal">
        <span class="surtitre">Comment ça se passe</span>
        <h2>Du premier jour au rythme de croisière, ce que je fais avec vous</h2>
      </div>

      <ol class="etapes espace-haut">
        {
          etapes.map((e, i) => (
            <li class="etape reveal" style={`--delai: ${i * 90}ms`}>
              <span class="etape-numero">{e.n}</span>
              <div class="etape-corps">
                <h3>{e.titre}</h3>
                <p>{e.texte}</p>
              </div>
            </li>
          ))
        }
      </ol>

      <div class="reveal centre-mobile espace-haut-petit">
        <a href="/methode" class="lien-fleche">
          Voir la méthode en détail
          <Icone nom="fleche" classe="fleche" />
        </a>
      </div>
    </div>
  </section>

  <!-- ===== POUR QUI ===== -->
  <section class="section section-teintee">
    <div class="rail resultat-rail">
      <div class="entete-section reveal">
        <span class="surtitre">À qui s’adresse Elyo Direct</span>
        <h2>Aux professionnels de l’accompagnement qui veulent un cabinet qui tourne</h2>
      </div>
      <ul class="liste-cochee resultat-liste reveal" style="--delai: 120ms">
        {pourQui.map((item) => <li>{item}</li>)}
      </ul>
    </div>
  </section>

  <!-- ===== TARIF ===== -->
  <section class="section" id="tarif">
    <div class="rail">
      <div class="entete-section reveal">
        <span class="surtitre">Le tarif</span>
        <h2>Une activation, puis un accompagnement mensuel</h2>
        <p class="chapeau">
          Le site est compris dans l’activation : il n’y a pas de facture séparée. Ensuite,
          l’accompagnement mensuel fait vivre et progresser ce qui a été mis en place.
        </p>
      </div>

      <div class="tarif espace-haut">
        <article class="tarif-carte tarif-carte-vedette reveal">
          <div class="tarif-duo">
            <div>
              <span class="tarif-libelle">Activation</span>
              <p class="tarif-prix">{prix.activation}</p>
              <p class="tarif-note">Une fois, au démarrage.</p>
            </div>
            <span class="tarif-plus" aria-hidden="true">puis</span>
            <div>
              <span class="tarif-libelle">Chaque mois</span>
              <p class="tarif-prix">{prix.mensuel}<small> / mois</small></p>
              <p class="tarif-note">Tant que vous le souhaitez.</p>
            </div>
          </div>
          <ul class="liste-cochee">
            <li>Votre activité et votre message mis au clair</li>
            <li>Site professionnel créé ou refondu, rapide et adapté au mobile</li>
            <li>Prise de rendez-vous, formulaire qualifiant, confirmations et relances</li>
            <li>Fiche Google, référencement local et mesure des demandes</li>
            <li>Puis chaque mois : suivi, améliorations et actions de développement</li>
          </ul>
          <div class="groupe-btn tarif-actions">
            <a href={lienDemarrer} class="btn btn-principal btn-large">
              Voir si Elyo Croissance est adapté à mon activité
              <Icone nom="fleche" classe="fleche" />
            </a>
            <a href="/rendez-vous" class="btn btn-secondaire btn-large">Demander un échange découverte</a>
          </div>
          <p class="tarif-mention">
            Les outils tiers et les éventuels budgets publicitaires restent à votre charge.
          </p>
        </article>
      </div>
    </div>
  </section>  <!-- ===== LES FORMULES MENSUELLES ===== -->
  <section class="section section-teintee" id="suite">
    <div class="rail">
      <div class="entete-section reveal">
        <span class="surtitre">L’abonnement mensuel</span>
        <h2>Vous choisissez votre niveau d’accompagnement</h2>
        <p class="chapeau">
          Après l’activation, vous décidez du rythme. On change de formule ou on s’arrête quand
          vous voulez — et vous gardez tout ce qui a été construit.
        </p>
      </div>

      <div class="formules espace-haut">
        {
          formules.map((fo, i) => (
            <article
              class={`formule carte reveal${fo.vedette ? ' formule-vedette' : ''}`}
              style={`--delai: ${i * 80}ms`}
            >
              {fo.vedette && <span class="formule-ruban">La plus choisie</span>}
              <h3 class="formule-nom">{fo.nom}</h3>
              <p class="formule-prix">
                {fo.prix}<small> / mois</small>
              </p>
              <p class="formule-resume">{fo.resume}</p>
            </article>
          ))
        }
      </div>

      <div class="reveal centre-mobile espace-haut-petit">
        <a href="/offre#suite" class="lien-fleche">
          Voir le détail des trois formules
          <Icone nom="fleche" classe="fleche" />
        </a>
      </div>
    </div>
  </section>

  <!-- ===== JULIEN ===== -->
  <section class="section section-teintee">
    <div class="rail julien-rail">
      <img
        class="julien-photo reveal"
        src="/images/julien-duplouy-220.webp"
        srcset="/images/julien-duplouy-220.webp 220w, /images/julien-duplouy-440.webp 440w"
        sizes="(min-width: 900px) 180px, 40vw"
        width="220"
        height="220"
        alt="Julien Duplouy, fondateur d’ElyoStudio"
        loading="lazy"
      />
      <div class="julien-texte reveal" style="--delai: 100ms">
        <span class="surtitre">Qui vous accompagne</span>
        <h2>Julien Duplouy, fondateur d’ElyoStudio</h2>
        <p>
          Plusieurs années en communication et marketing digital, puis le même constat répété chez
          les indépendants de l’accompagnement : compétents, appréciés, invisibles. J’ai créé
          ElyoStudio pour leur apporter ce qui manque — un système complet, un prix affiché, et
          quelqu’un à qui parler quand une décision bloque.
        </p>
        <p>
          Je démarre cet accompagnement avec mes premiers cabinets. Je ne vous promets pas un
          agenda plein en 90 jours : je vous promets des fondations solides, un travail sérieux,
          et une présence réelle, mois après mois.
        </p>
        <a href="/a-propos" class="lien-fleche">
          En savoir plus sur ma façon de travailler
          <Icone nom="fleche" classe="fleche" />
        </a>
      </div>
    </div>
  </section>

  <!-- ===== FAQ ===== -->
  <section class="section">
    <div class="rail-etroit">
      <div class="entete-section reveal">
        <span class="surtitre">Questions fréquentes</span>
        <h2>Ce que l’on me demande avant de démarrer</h2>
      </div>

      <div class="faq espace-haut">
        {
          faqs.map((f, i) => (
            <details class="reveal" style={`--delai: ${i * 45}ms`}>
              <summary>{f.q}</summary>
              <p class="reponse">{f.a}</p>
            </details>
          ))
        }
      </div>
    </div>
  </section>

  <BandeCta />
</Base>

<style>
  /* --- Héros, bleu nuit --- */
  .heros {
    position: relative;
    overflow: hidden;
    color: #FFFFFF;
    background:
      radial-gradient(90% 70% at 100% 0%, rgba(43, 65, 120, 0.9), transparent 60%),
      linear-gradient(160deg, #0F1A33 0%, #1C2E57 62%, #22396B 100%);
  }
  .heros-arc {
    position: absolute;
    right: -12rem;
    top: -14rem;
    width: 40rem;
    height: 40rem;
    border-radius: 50%;
    border: 1px solid rgba(255, 255, 255, 0.12);
    pointer-events: none;
  }
  .heros-rail {
    position: relative;
    z-index: 1;
    display: grid;
    gap: clamp(2rem, 6vw, 3.5rem);
    padding-block: clamp(2.8rem, 8vw, 5rem) clamp(3rem, 9vw, 5.5rem);
  }
  .heros-surtitre {
    display: inline-block;
    font-size: 0.74rem;
    font-weight: 700;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.72);
  }
  .heros-texte h1 {
    margin-top: 1.2rem;
    color: #FFFFFF;
    font-size: clamp(2.2rem, 6.2vw, 3.7rem);
    line-height: 1.04;
  }
  .heros-chapeau {
    margin-top: 1.4rem;
    max-width: 34rem;
    font-size: clamp(1.02rem, 2.2vw, 1.15rem);
    line-height: 1.7;
    color: rgba(255, 255, 255, 0.82);
  }
  .heros-texte .groupe-btn { margin-top: 2rem; }

  @media (min-width: 1024px) {
    .heros-rail { grid-template-columns: 1.05fr 0.95fr; align-items: center; gap: 3rem; }
  }

  /* --- Bandeau --- */
  .chiffres { border-block: 1px solid var(--ligne); background: var(--blanc); }
  .chiffres-grille { display: grid; gap: 1.4rem; padding-block: clamp(1.8rem, 5vw, 2.6rem); }
  .chiffre { display: grid; gap: 0.35rem; }
  .chiffre strong {
    font-family: var(--police-titre);
    font-size: clamp(1.5rem, 4vw, 1.95rem);
    font-weight: 600;
    color: var(--vert);
  }
  .chiffre span { font-size: 0.93rem; color: var(--encre-2); }
  @media (min-width: 760px) { .chiffres-grille { grid-template-columns: repeat(3, 1fr); gap: 2rem; } }

  /* --- Utilitaires --- */
  .espace-haut { margin-top: clamp(2rem, 5vw, 3rem); }
  .espace-haut-petit { margin-top: 1.6rem; }
  .centre-mobile { display: flex; justify-content: flex-start; }

  /* --- Résultat / pour qui --- */
  .resultat-rail { display: grid; gap: clamp(1.5rem, 4vw, 3rem); align-items: start; }
  .resultat-liste { gap: 0.9rem; font-size: 1.02rem; }
  @media (min-width: 900px) {
    .resultat-rail { grid-template-columns: 1.1fr 0.9fr; }
    .resultat-rail .entete-section { margin-bottom: 0; }
  }

  /* --- Elyo Direct --- */
  .section-sombre {
    position: relative;
    overflow: hidden;
    color: #FFFFFF;
    background: linear-gradient(150deg, #0F1A33 0%, #1C2E57 60%, #22396B 100%);
  }
  .direct-halo {
    width: 26rem; height: 26rem;
    top: -10rem; right: -8rem;
    background: rgba(217, 160, 106, 0.22);
  }
  .direct-rail { position: relative; z-index: 1; display: grid; gap: clamp(2rem, 5vw, 3.5rem); }
  .surtitre-clair { color: var(--ambre); }
  .direct-texte h2 { color: #FFFFFF; }
  .direct-chapeau {
    margin-top: 1.1rem;
    max-width: 34rem;
    color: rgba(255, 255, 255, 0.82);
    line-height: 1.7;
  }
  .direct-situations {
    list-style: none;
    padding: 0;
    margin-top: 1.4rem;
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }
  .direct-situations li {
    padding: 0.45rem 0.9rem;
    border-radius: var(--r-pilule);
    border: 1px solid rgba(255, 255, 255, 0.22);
    background: rgba(255, 255, 255, 0.08);
    font-size: 0.88rem;
  }
  .direct-canaux { display: grid; gap: 0.9rem; }
  .direct-canal {
    display: grid;
    grid-template-columns: auto 1fr;
    gap: 1rem;
    align-items: start;
    padding: 1.1rem 1.2rem;
    border-radius: var(--r-l);
    border: 1px solid rgba(255, 255, 255, 0.14);
    background: rgba(255, 255, 255, 0.06);
  }
  .direct-icone {
    display: grid;
    place-items: center;
    width: 2.6rem;
    height: 2.6rem;
    border-radius: var(--r-m);
    background: rgba(217, 160, 106, 0.18);
    color: var(--ambre);
  }
  .direct-icone :global(svg) { width: 1.3rem; height: 1.3rem; }
  .direct-canal h3 { color: #FFFFFF; font-size: 1.05rem; margin-bottom: 0.3rem; }
  .direct-canal p { color: rgba(255, 255, 255, 0.76); font-size: 0.93rem; }
  @media (min-width: 960px) {
    .direct-rail { grid-template-columns: 1.1fr 0.9fr; align-items: center; }
  }

  /* --- Étapes --- */
  .etapes { list-style: none; padding: 0; display: grid; gap: 1rem; }
  .etape {
    display: grid;
    grid-template-columns: auto 1fr;
    gap: 1.1rem;
    align-items: start;
    padding: clamp(1.2rem, 3.4vw, 1.7rem);
    background: var(--blanc);
    border: 1px solid var(--ligne);
    border-radius: var(--r-l);
    box-shadow: var(--ombre-s);
    transition: transform var(--transition), box-shadow var(--transition);
  }
  .etape:hover { transform: translateY(-3px); box-shadow: var(--ombre-m); }
  .etape-numero {
    display: grid;
    place-items: center;
    width: 2.9rem;
    height: 2.9rem;
    border-radius: var(--r-m);
    background: linear-gradient(150deg, var(--vert), #0F1A33);
    color: #FFFFFF;
    font-family: var(--police-titre);
    font-size: 0.95rem;
    font-weight: 600;
  }
  .etape-corps h3 { margin-bottom: 0.4rem; }
  .etape-corps p { color: var(--encre-2); font-size: 0.97rem; }
  @media (min-width: 900px) { .etapes { grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 1.15rem; } }

  /* --- Tarif --- */
  .tarif { display: grid; max-width: 44rem; margin-inline: auto; }
  .tarif-carte {
    position: relative;
    padding: clamp(1.6rem, 4vw, 2.4rem);
    background: var(--blanc);
    border: 1px solid var(--ligne);
    border-radius: var(--r-xl);
    box-shadow: var(--ombre-l);
  }
  .tarif-carte-vedette { border-color: rgba(192, 102, 60, 0.35); }
  .tarif-ruban {
    position: absolute;
    top: -0.85rem;
    left: clamp(1.6rem, 4vw, 2.4rem);
    padding: 0.35rem 0.9rem;
    border-radius: var(--r-pilule);
    background: linear-gradient(150deg, var(--terre), var(--terre-2));
    color: #FFF6F1;
    font-size: 0.72rem;
    font-weight: 700;
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }
  .tarif-entete { padding-bottom: 1.3rem; margin-bottom: 1.4rem; border-bottom: 1px solid var(--ligne); }
  .tarif-libelle {
    font-size: 0.76rem;
    font-weight: 700;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--vert);
  }
  .tarif-prix {
    margin-top: 0.5rem;
    font-family: var(--police-titre);
    font-size: clamp(2.6rem, 8vw, 3.6rem);
    font-weight: 600;
    line-height: 1;
    color: var(--encre);
  }
  .tarif-note { margin-top: 0.7rem; color: var(--encre-2); font-size: 0.95rem; }
  .tarif-actions { margin-top: 1.6rem; }
  .tarif-mention { margin-top: 1rem; font-size: 0.84rem; color: var(--encre-3); }

  /* --- Julien --- */
  .julien-rail { display: grid; gap: 1.6rem; align-items: center; }
  .julien-photo {
    width: min(11rem, 45vw);
    height: auto;
    border-radius: 50%;
    box-shadow: 0 0 0 4px var(--blanc), 0 0 0 5px rgba(212, 178, 122, 0.6), var(--ombre-m);
  }
  .julien-texte h2 { margin-bottom: 0.9rem; }
  .julien-texte p { color: var(--encre-2); margin-bottom: 0.9rem; max-width: 40rem; }
  .julien-texte .lien-fleche { margin-top: 0.4rem; }
  @media (min-width: 800px) {
    .julien-rail { grid-template-columns: auto 1fr; gap: 2.5rem; }
  }

  /* --- Ce que nous installons (section sombre) --- */
  .socle-grille { display: grid; gap: 0.9rem; }
  .socle-carte {
    display: grid;
    grid-template-columns: auto 1fr;
    gap: 1rem;
    align-items: start;
    padding: 1.1rem 1.2rem;
    border-radius: var(--r-l);
    border: 1px solid rgba(255, 255, 255, 0.14);
    background: rgba(255, 255, 255, 0.06);
  }
  .socle-carte h3 { color: #ffffff; font-size: 1.02rem; margin-bottom: 0.3rem; }
  .socle-carte p { color: rgba(255, 255, 255, 0.76); font-size: 0.92rem; }
  .titre-clair { color: #ffffff; }
  @media (min-width: 760px) { .socle-grille { grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 1rem; } }

  /* --- Tarif : activation + mensuel --- */
  .tarif-duo {
    display: grid;
    gap: 0.9rem;
    align-items: center;
    padding-bottom: 1.3rem;
    margin-bottom: 1rem;
    border-bottom: 1px solid var(--ligne);
  }
  .tarif-plus {
    font-family: var(--police-titre);
    font-size: 1.1rem;
    font-style: italic;
    color: var(--encre-3);
  }
  .tarif-prix small { font-size: 1rem; font-weight: 500; color: var(--encre-3); }
  .tarif-somme { margin-bottom: 1.4rem; font-size: 1rem; color: var(--encre-2); }
  .tarif-somme strong { color: var(--encre); font-family: var(--police-titre); font-size: 1.15rem; }
  @media (min-width: 640px) { .tarif-duo { grid-template-columns: 1fr auto 1fr; gap: 1.2rem; } }  /* --- Formules mensuelles --- */
  .formules { display: grid; gap: 1.1rem; align-items: stretch; }
  .formule { position: relative; display: flex; flex-direction: column; }
  .formule-vedette { border-color: rgba(192, 102, 60, 0.35); box-shadow: var(--ombre-l); }
  .formule-ruban {
    position: absolute;
    top: -0.8rem;
    left: clamp(1.2rem, 4vw, 1.8rem);
    padding: 0.3rem 0.85rem;
    border-radius: var(--r-pilule);
    background: linear-gradient(150deg, var(--terre), var(--terre-2));
    color: #fff6f1;
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }
  .formule-nom { font-size: 1.15rem; }
  .formule-prix {
    margin-top: 0.4rem;
    font-family: var(--police-titre);
    font-size: clamp(1.8rem, 5vw, 2.2rem);
    font-weight: 600;
    line-height: 1;
    color: var(--vert);
  }
  .formule-prix small { font-size: 0.88rem; font-weight: 500; color: var(--encre-3); }
  .formule-resume { margin-top: 0.7rem; color: var(--encre-2); font-size: 0.95rem; }
  @media (min-width: 900px) { .formules { grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 1.2rem; } }
</style>
